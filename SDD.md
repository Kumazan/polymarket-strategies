# SDD: Add .gitignore for static site

## Goal
Prevent OS metadata, editor files, and potential future tooling artifacts from polluting `git status`.

## Scope

**In:**
- `.gitignore` — new file, 18 lines
- `SDD.md` — this design doc

**Out:**
- No changes to `index.html`, images, or `README.md`
- No dependency changes

## Approach
Standard `.gitignore` for a static HTML site:
- macOS: `.DS_Store`, `.AppleDouble`, `.LSOverride`
- Editor: `.vscode/`, `.idea/`, swap files
- Node (future-proof): `node_modules/`, `dist/`, `build/`
- Env: `.env*`

**Alternatives considered:**
- Using a global `.gitignore_global` — rejected: project-level is more portable and self-documenting

**Trade-offs:** None significant. Documentation-only change.

## Change List
| File | Module | Why |
|------|--------|-----|
| `.gitignore` | repo root | Prevent artifact pollution |
| `SDD.md` | repo root | Design doc for this change |

## Tests
Static file — no build pipeline.

```bash
git check-ignore .DS_Store   # ✅ matched
git check-ignore node_modules # ✅ matched
git status                   # ✅ clean (only new files in staging)
```

## Rollback
Delete `.gitignore` — zero runtime impact.

## Risk Notes
None. Only affects untracked files.

---
*Auto-generated by OpenClaw github-pr-autopilot @ 2026-03-28 04:00 Asia/Taipei*
*Size: Small (1 file added, 18 lines)*
